# Migration 20260509_init

This migration initializes the PostgreSQL schema for BackendTalex and includes all Prisma models defined in `prisma/schema.prisma`.

Run:

```bash
npx prisma migrate deploy
```
